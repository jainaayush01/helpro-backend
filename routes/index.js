const internships = require("./internships");
const jobs = require("./jobs");
const hackathons = require("./hackathons");
const scripts = require("./scripts");

module.exports = { internships, jobs, hackathons, scripts };
